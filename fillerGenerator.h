// Print a title with a line of dashes underneath
void printTitle (char *title);

// Get user input for the number of words to generate
int getWords (void);

// Add a randomly generated word to the sequental file
void addWord (void);
